# Implementation Plan — Story #52: Legal Minimum Driver Age (21 years)

**Issue:** #52  
**Milestone:** v0.3-legal-age-update  
**Date:** 2026-05-30  
**Approach:** Outside-In TDD — RED → GREEN → REFACTOR  
**Walking Skeleton Strategy:** A (full in-memory, no infrastructure dependency)

---

## Outside-In Order

### Step 1 — RED: Acceptance tests (Application layer)

**File to create / modify:**  
`tests/MonAssurance.UnitTests/Eligibility/Application/CheckEligibilityQueryHandlerTests.cs`

**Tests to write (new or updated):**

| Test name | AC | Expected result |
|---|---|---|
| `Handle_WhenDriverIs20AndHasCar_ReturnsRefused` | AC-01 | `IsEligible = false`, `RejectionReason = "Conducteur trop jeune pour ce véhicule"` |
| `Handle_WhenDriverIs21AndHasCar_ReturnsEligible` | AC-02 | `IsEligible = true`, `RejectionReason = null` |
| `Handle_WhenDriverIs20AndHasMotorcycle_ReturnsRefused` | AC-03 | `IsEligible = false`, `RejectionReason = "Conducteur trop jeune pour ce véhicule"` |
| `Handle_WhenDriverIs18AndHasCar_ReturnsRefused` | AC-04 | `IsEligible = false`, `RejectionReason = "Conducteur trop jeune pour ce véhicule"` — replaces the old `_ReturnsEligible` test |
| `Handle_WhenDriverIs16AndHasElectricScooter_ReturnsEligible` | AC-05 | `IsEligible = true`, `RejectionReason = null` — existing test, must remain green |
| `Handle_WhenDriverIs21AndHasMotorcycle_ReturnsEligible` | AC-03 (boundary) | `IsEligible = true`, `RejectionReason = null` — covers the 21/Motorcycle/eligible Scenario Outline row |

> **Note:** The existing test `Handle_WhenDriverIs18AndHasCar_ReturnsEligible` asserts the OLD 18-year rule.  
> That test MUST be updated to assert `IsEligible = false` to reflect the new law.  
> Rename it to `Handle_WhenDriverIs18AndHasCar_ReturnsRefused` for clarity.

**Enters through:** `CheckEligibilityQueryHandler.Handle(CheckEligibilityQuery)` — the single Application layer entry point.  
**Double type:** None — `CheckEligibilityQueryHandler` is instantiated directly with `new EligibilityPolicy()` and `FakeTimeProvider`. No repository involved.

---

### Step 2 — GREEN: Domain production change

**File to modify:**  
`src/MonAssurance.Domain/Eligibility/Vehicle.cs`

**Change:**

```diff
- public int MinimumAge() => _type == VehicleType.ElectricScooter ? 16 : 18;
+ public int MinimumAge() => _type == VehicleType.ElectricScooter ? 16 : 21;
```

This is the single line change that drives all 5 ACs to green.

---

### Step 3 — VERIFY: Existing test suite remains green

After Step 2, run the full unit test suite:

```bash
dotnet test tests/MonAssurance.UnitTests
```

- All updated tests from Step 1 must be green.
- The scooter test (`Handle_WhenDriverIs16AndHasElectricScooter_ReturnsEligible`) must remain green — the `ElectricScooter` branch is unchanged.
- All high-power motorcycle experience tests must remain green — the `IsHighPowerMotorcycle()` rule is independent.

---

### Step 4 — VERIFY: Integration test suite remains green

```bash
dotnet test tests/MonAssurance.IntegrationTests
```

Confirms that architecture rules, DI wiring, and endpoint contracts are unaffected.

---

## Scope Boundaries

| In scope | Out of scope |
|---|---|
| `Vehicle.MinimumAge()` → `21` for Car/Motorcycle | High-power motorcycle experience rule |
| Updating existing unit tests asserting the 18-year threshold | Electric scooter minimum age (16) — governed by issue #45 |
| All 5 ACs green in `CheckEligibilityQueryHandlerTests.cs` | Any new domain concept or new file |

---

## No Domain Test Extraction

Per Mandate 4 (M4 negative — saturated by AC): `Vehicle.MinimumAge()` has 2 behavioral branches, both covered by planned acceptance tests. No domain unit test is authorized for this story.

---

_Generated by acceptance-designer — 2026-05-30_  
_Issue: #52 | Iteration: 2_
